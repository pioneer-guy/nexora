import {NextResponse} from 'next/server';
import {generateAIResponse,parseAIJson} from '../../../../lib/ai/provider';
export async function POST(req){try{const {target,questions,answers}=await req.json();if(!target||!Array.isArray(questions)||!questions.length)return NextResponse.json({error:'Resume questions and answers are required.'},{status:400});const response=await generateAIResponse({messages:[{role:'system',content:`Evaluate these answers only against the resume-specific questions for ${target}. Do not reward invented experience. Empty, evasive, or unrelated answers should score low but respond constructively. Return JSON only: {"score":0,"understanding":0,"practicalAbility":0,"problemSolving":0,"roleReadiness":0,"status":"Demonstrated|Needs Evidence","summary":"string","strengths":["string"],"improvements":["string"],"nextStep":"string"}. Scores are 0-100 and must be justified by submitted answers.`},{role:'user',content:JSON.stringify({target,questions,answers})}],temperature:.2});const result=parseAIJson(response);if(!result)return NextResponse.json({error:'The AI returned an unreadable evaluation. Try again.'},{status:502});return NextResponse.json(result)}catch(error){console.error('[NEXORA resume grading]',error);return NextResponse.json({error:'Answer evaluation failed. Check your AI provider configuration.'},{status:503})}}
