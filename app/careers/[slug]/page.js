import Link from 'next/link';
import {notFound} from 'next/navigation';
import {getCareer} from '../../../lib/careers';
export default async function CareerPage({params}){const career=getCareer((await params).slug);if(!career)notFound();return <><nav><Link className="logo" href="/">NEX<span>ORA</span></Link><div className="links"><Link href="/careers">Careers</Link><Link href="/trending">Trending</Link><Link href="/journey">My Journey</Link><Link href="/proofwork">ProofWork</Link></div></nav><main className="workspace"><Link className="back" href="/careers">← Back to Careers</Link><small>{career.category} · CAREER PATH</small><h1>Your path to <i>{career.title}</i></h1><p className="lead left">{career.description}</p><section className="detailGrid"><div><small>WHAT DOES THIS ROLE ACTUALLY DEMAND?</small><h2>Work that creates <i>evidence.</i></h2><div className="detailList">{career.responsibilities.map(item=><p key={item}>✓ {item}</p>)}</div><h3>Core skills</h3><div className="tagList">{career.skills.map(item=><span key={item}>{item}</span>)}</div><h3>Tools commonly used</h3><div className="tagList">{career.tools.map(item=><span key={item}>{item}</span>)}</div></div><aside className="sidePanel"><small>BEGINNER REQUIREMENTS</small>{career.beginner.map(item=><p key={item}>→ {item}</p>)}<Link className="primary full" href={`/journey?career=${career.slug}`}>START THE GOAL</Link></aside></section><section className="roadmap"><small>YOUR LEARNING PATH</small><h2>Understand → Learn → Practice → Build → <i>Prove</i></h2><div className="stageGrid">{career.stages.map((stage,index)=><article key={stage.stage}><em>0{index+1}</em><h3>{stage.stage}</h3>{stage.topics.map(topic=><Link href={`/learn/${career.slug}?topic=${encodeURIComponent(topic)}`} key={topic}>{topic}<span>Start learning →</span></Link>)}{stage.stage==='Prove'&&<Link className="secondary full" href={`/proofwork?career=${career.slug}`}>Test your skill</Link>}</article>)}</div></section></main><footer><b>NEX<span>ORA</span></b><span>Know the path. Prove the skill.</span></footer></>}
